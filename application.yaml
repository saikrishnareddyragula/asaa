spring:
  config:
    activate:
      on-profile:
  datasource:
    password: root
    url: jdbc:postgresql://localhost:5432/customers
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
